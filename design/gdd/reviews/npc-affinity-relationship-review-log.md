# Review Log — NPC Affinity & Relationship (#9)

## Review — 2026-08-08 — Verdict: NEEDS REVISION
Scope signal: L (5 công thức, 9 dependency, 18 tuning knob, state machine 5 trạng thái, 1 rename hợp đồng liên-GDD; không cần ADR mới)
Specialists: game-designer, systems-designer, economy-designer, qa-lead, creative-director (senior synthesis)
Blocking items: 8 | Recommended: ~14
Summary: Phát hiện cấu trúc quan trọng nhất (creative-director): hệ có 3 lớp kỷ luật số học (D.2 diminishing / D.3 fatigue / D.4 cap) nhưng đúng con đường người chơi thực sự dùng (Song Tu, lan truyền qua NPC bị ghét) lại được miễn trừ khỏi cả 3 — "tính phí cho roleplay, trợ giá cho bấm nút", ngược tinh thần Player Fantasy. 3 specialist độc lập hội tụ vào cùng 1 kết luận: Song Tu là dominant strategy Pareto-superior (tự mâu thuẫn giữa Core Rule #6 và ràng buộc gate ở Dependencies; miễn trừ D.2-D.4 VÀ free-ride EXP tick action-agnostic của `exp-realm-progression.md` D.6). economy-designer phát hiện thêm 1 khai thác kinh tế độc lập: lan truyền qua NPC bị ghét (`link_strength` âm) tạo dòng Hảo cảm dương miễn phí, vĩnh viễn, miễn trừ fatigue — vì chi phí trực tiếp bị giới hạn ở -100 trong khi thu nhập lan truyền không giới hạn. systems-designer tìm 3 lỗi pseudocode-vs-prose (D.6 thiếu guard miễn trừ Song Tu, nhánh witness thiếu `CRUELTY_REP_DELTA`, rủi ro int/int truncation cho `margin_ratio`) và 1 lệch interface `SONG_TU_ACTIVE` giữa 2 GDD. qa-lead tìm 2 gap AC (input space của property-based test AC-11 chưa đủ chi tiết; AC-25 khẳng định "terminal" bằng lời văn nhưng không test). Phân loại: ~4 nhóm-A (Song Tu gate + số phận D.3; khai thác lan truyền; interface `SONG_TU_ACTIVE`) vs ~11 nhóm-B (đa số) — Round Cap tiêu chuẩn áp dụng (KHÔNG phải amendment economy-derivation-gated), đây là vòng 1/2, vòng 2 là vòng cuối.
Prior verdict resolved: First review

## Revision — 2026-08-08 — Sửa ngay trong cùng phiên (theo lựa chọn người dùng: "Sửa GDD ngay")

2 quyết định thiết kế được chốt qua `AskUserQuestion` trước khi sửa:
1. Song Tu gate + số phận D.3 → **Gate Song Tu (thêm `SONG_TU_COOLDOWN_TURNS`) + GIỮ D.3** (nâng `FATIGUE_WINDOW_TURNS` 3→5 để D.3 thật sự kích hoạt, thay vì phương án khuyến nghị của creative-director là cắt D.3 khỏi MVP).
2. Chặn khai thác lan truyền → **Gắn lan truyền vào hiệu lực trực tiếp còn sống** (nếu nạn nhân đã bão hòa ở -100, propagation không kích hoạt nữa) — đúng phương án khuyến nghị của creative-director.

| # | Blocking item | Fix applied |
|---|---|---|
| 1 | Core Rule #6 tự mâu thuẫn với Dependencies (Song Tu gate) + Song Tu là dominant strategy Pareto-superior | Core Rule #6 viết lại: thêm điều kiện (b) `SONG_TU_COOLDOWN_TURNS` (mặc định 5) per-NPC, tracker `last_song_tu_turn(npc_id)` mới, bấm khi chưa đủ điều kiện bị từ chối trước Turn Manager. States table + UI Requirements cập nhật hiển thị đếm ngược. AC-07b mới. |
| 2 | Interface `SONG_TU_ACTIVE` lệch nghĩa giữa 2 GDD (thiếu vế Tâm Pháp) | Core Rule #7 đổi tên export: hệ này xuất `song_tu_relationship_active_npc_ids` (danh sách), KHÔNG sở hữu tên `SONG_TU_ACTIVE` — đó là biến nội bộ D.4 của `exp-realm-progression.md`. Cascade: `exp-realm-progression.md` Open Questions viết lại tường minh D.4 = AND(Tâm Pháp, danh sách ≠ ∅); `design/registry/entities.yaml` entry `song_tu_active` đổi `source` sang exp-realm-progression.md, note lịch sử sửa. Mọi tham chiếu `SONG_TU_ACTIVE` trong GDD này (Edge Cases, AC-09, AC-29, Dependencies) đổi sang tên mới hoặc làm rõ đây là biến của EXP. |
| 3 | D.6 pseudocode thiếu guard loại `song_tu_action` khỏi nhân diminish×fatigue (mâu thuẫn Core Rule #6 + AC-07) | Thêm nhánh `ELSE IF event.type == song_tu_action` tường minh trong D.6 B1, tách khỏi nhánh chung |
| 4 | D.6 nhánh witness thiếu `CRUELTY_REP_DELTA` (mâu thuẫn D.5 prose + AC-18/AC-20) | Sửa D.6 B1: `contributions[w] += base_delta(event) + CRUELTY_REP_DELTA` |
| 5 | `margin_ratio = hp_after/max_HP` rủi ro int/int truncation (thiếu ép kiểu float, cùng lớp lỗi combat-system.md D.9b) | Sub-formula D.1 + Variables table: `float(hp_after)/max(max_HP,1)`, ép kiểu tường minh. AC-13b mới test bằng input int thật (không mock sẵn float) |
| 6 | Khai thác lan truyền qua NPC bị ghét: chi phí trực tiếp giới hạn ở -100, thu nhập lan truyền không giới hạn, miễn trừ fatigue | D.5 + D.6 B2: thêm điều kiện `A_before(victim) > -100 OR event.type == kill_witnessed` trước khi propagation kích hoạt. Edge Case mới. AC-19b mới (regression cho khai thác đã đóng) |
| 7 | AC-11 (property-based clamp test, đóng rủi ro clamp High-Risk) không đặc tả đủ input space | Viết lại đầy đủ: event-type sample space, `linked_npcs` size/distribution, `witnesses` subset, `perpetrator_known` suy ra đúng công thức — không còn chỗ đoán |
| 8 | AC-25 khẳng định "Ended terminal" bằng lời văn, không có bước test verify | Thêm bước: raise affinity lại sau khi chết, verify state VẪN Ended, nút vẫn ẩn |

Recommended đã tiện thể đóng (chi phí thấp, nằm cùng vùng sửa blocking): câu Player Fantasy "không có free kill nào sạch sẽ" (mâu thuẫn Core Rule #4 đã chốt 2026-08-03) — sửa lại chính xác phạm vi (chỉ đúng cho xung đột không phải giết, hoặc giết có nhân chứng); Open Question "content-gating chống ratchet" viết lại phản ánh phần đã đóng (Song Tu gate tự sở hữu, khai thác lan truyền đã chặn) vs phần còn mở (Situation Gen tự nguyện tuân thủ "không positive event on-demand"); cascade `situation-encounter-generation.md` Tuning Knobs — thêm ghi chú bất biến `POSITIVE_SOCIAL_COOLDOWN_TURNS ≤ FATIGUE_WINDOW_TURNS`.

Recommended CHƯA đóng trong lần sửa này (còn nguyên trong Recommended Revisions của báo cáo review, dành cho round 2 hoặc backlog theo Round Cap): fatigue vô hình với người chơi (không tín hiệu tường thuật); Song Tu Active nhưng nút ẩn (40≤A<60) không có lời giải thích UI; "Tri kỷ hy sinh vì NVC" không có cơ chế; `link_strength` tĩnh không phản ánh pillar "NPC là NVC của chính họ"; nhiều Song Tu không có hệ quả ghen tuông; pacing claim "8-12 hành động" lệch ~2x; diminishing returns khuyến khích dàn trải NPC hơn đầu tư sâu; streak tracker không có eviction; `CRUELTY_REP_DELTA` prose "mọi NPC biết tin" hơi quá phạm vi thật; thiếu AC cho ranh giới 7 dải thái độ, break threshold + thù địch sâu sắc đồng thời, propagation severity=3 không cần nhân chứng end-to-end; dev-seed NPC ở game-concept.md nên là +75 thay vì +60 (game-designer B3, tránh va chạm combat MVP).

Trạng thái sau sửa: **Designed — Revised, chờ re-review (vòng 2/2 — round cap mechanically-heavy, vòng 2 là vòng cuối)**. Theo `.claude/docs/coordination-rules.md`, vòng 2 nên chạy như audit có mục tiêu (Core Rule #6, D.2-D.6, các AC liên quan, dòng dependency Situation Gen, D.4 của `exp-realm-progression.md`) — không phải 1 lượt adversarial toàn văn bản mới. Khuyến nghị `/clear` trước khi re-review.

## Review — 2026-08-08 — Verdict: NEEDS REVISION (minor) → APPROVED cùng phiên
Scope signal: L (không đổi từ vòng 1 — 5 công thức, 9 dependency, 18 tuning knob, state machine 5 trạng thái, không cần ADR mới)
Specialists: game-designer, systems-designer, economy-designer, qa-lead (audit có mục tiêu, đúng scope review log vòng 1 yêu cầu) + creative-director (senior synthesis)
Blocking items: 4 | Recommended: 4 (áp dụng cùng phiên) + ~8 backlog không đổi từ vòng 1
Summary: Vòng cuối của round cap — không có vòng 3. Cả 4 blocking đều là gap đồng bộ tài liệu phát sinh từ ĐÚNG 1 thay đổi hằng số ở vòng 1 (`FATIGUE_WINDOW_TURNS` 3→5), không phải design defect mới: (1) [game-designer] Player Fantasy khẳng định tuyệt đối "không có cách nào gây hại sạch sẽ hoàn toàn" bị chính saturation gate D.5/D.6 B2 (thêm ở vòng 1) phủ định — nạn nhân còn sống đã bão hòa -100 nhận hành vi thù địch tiếp theo mà không hậu quả cơ học nào; (2) [systems-designer] `design/registry/entities.yaml` entry `resolve_turn_affinity` stale — vẫn mô tả hành vi TRƯỚC KHI vá (ghi "3 lượt" thay vì 5, thiếu điều kiện saturation gate, `revised: ""`); (3) [creative-director, 4 specialist bỏ sót] AC-16 pin fixture `WINDOW=3` — cấu hình mà chính GDD cấm (vi phạm bất biến liên-GDD `FATIGUE_WINDOW_TURNS ≥ POSITIVE_SOCIAL_COOLDOWN_TURNS`), mã hóa chính dead-code bug đã đóng ở vòng 1 thành 1 test PASS trong CI — AC ở gate BLOCKING; (4) preamble Formulas echo "3 lượt". economy-designer độc lập kiểm tra rủi ro round-robin đa-NPC Song Tu (game-designer nêu: cooldown "làm chậm chứ không xoá" dominance per-NPC) và XÁC NHẬN KHÔNG phải exploit mới — per-instance cooldown check against global one-action-per-turn budget, round-robin chỉ lấp lượt rảnh, không nhân throughput. systems-designer verify độc lập 4 boundary case (kill_witnessed 0-witness × B2, song_tu_action × B2, A_before=-99 transition, Undo × cooldown) — tất cả ĐÚNG, không có lỗi số học/logic mới. qa-lead tìm 4 gap coverage AC (không blocking, backlog). creative-director hợp nhất 2 backlog item của game-designer ("round-robin risk" + "Song Tu không hệ quả ghen tuông") thành 1 hạng mục P1 duy nhất với trigger đo được (≥2 NPC đồng thời đạt +60), đề xuất cơ chế ghen tuông dùng `link_strength` sẵn có — đóng cả lỗ hổng fiction lẫn kinh tế cùng lúc, rẻ hơn "global cooldown" thuần balance.
Người dùng chọn: sửa toàn bộ 4 blocking + 4 recommended liên quan (AC-08 mốc lượt, AC-19b dời đúng mục, AC-16b test tĩnh bất biến liên-GDD, pacing ước tính lại ~25-30 hành động) ngay trong phiên, theo đúng khuyến nghị creative-director. Không có quyết định thiết kế nào cần AskUserQuestion riêng — toàn bộ là fix đồng bộ, không phải trade-off.
Prior verdict resolved: Yes — toàn bộ 8 blocking của vòng 1 xác nhận đứng vững qua audit vòng 2 (không phát hiện lỗi số học/logic nào sống sót); 4 blocking mới của vòng 2 đều là propagation gap của vòng 1, không phải regression thiết kế.

**GDD status: Designed → Approved.** Round cap kết thúc — không có vòng 3.
