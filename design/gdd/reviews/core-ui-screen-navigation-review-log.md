# Review Log — Core UI / Screen Navigation (#15)

## Review — 2026-08-04 — Verdict: NEEDS REVISION
Scope signal: XL (11 dependency, 6 formula, 5+ tầng story, ≥3 ADR mới dự kiến)
Specialists: game-designer, systems-designer, qa-lead, ux-designer, ui-programmer, godot-specialist, accessibility-specialist, creative-director (senior synthesis)
Blocking items: 10 | Recommended: 10
Summary: Mô hình 3 tầng và formula D.1/D.2 (điều hướng) được duyệt nguyên trạng — kiến trúc đúng, không cần thiết kế lại. Blocking phân bố: 2 mục thuộc GDD khác (#5 World Memory tự mâu thuẫn nội bộ dòng 125 vs 437; #13 lệch tên interface `reset_in_progress`), 1 khoảng trống thiết kế thật (Màn chơi chính S2 không có giới hạn bộ đệm văn bản, không như Story Log có D.3), 1 quyết định UX cần chốt (3 bút tích điều hướng không sticky ở cả 2 đầu luồng cuộn — điều hướng toàn cục nằm ngoài màn hình phần lớn thời gian chơi), 6 mục còn lại là vệ sinh đặc tả (đếm AC sai 21 vs 25 thực tế, D.6 tuning knob range tự mâu thuẫn với chính invariant nó phục vụ, D.4 range sai khi glyph >44px, thiếu contrast/alpha số cụ thể, thiếu test âm-tính cho guard `new_slot_created`, thiếu cảnh báo kỹ thuật Web export cho ADR screen stack). 3 điểm bất đồng giữa specialist được creative-director phân xử tường minh (D.4 over-engineering hay cần thiết; mức độ nghiêm trọng của gap discoverability; lỗi thuộc #15 hay thuộc #5/#13).
Prior verdict resolved: First review

## Revision — 2026-08-04 — Sửa ngay trong cùng phiên (theo lựa chọn người dùng: "Sửa GDD ngay")

4 quyết định thiết kế được chốt qua `AskUserQuestion` (đều theo phương án Recommended của creative-director) trước khi sửa:
1. World Memory (#5) tự mâu thuẫn → **sửa cả 2 file** (không chỉ ghi Open Question).
2. Giới hạn bộ đệm S2 → **cửa sổ N lượt gần nhất** (D.3b mới, `live_window_turns=15`), dẫn sang Story Log khi chạm biên.
3. Khả năng-với-tới 3 bút tích → **nhân bản xuống cạnh khu nhập hành động** (đáy luồng cuộn), giữ nguyên bản ở header (đỉnh).
4. Giá trị contrast/alpha → **đặt placeholder ngay** (đạt ≥4.5:1 WCAG AA), gắn nhãn "art-director xác nhận khi khóa màu".

| # | Blocking item | Fix applied |
|---|---|---|
| 1 | World Memory (#5) tự mâu thuẫn (dòng 125 vs 437) | Sửa `world-memory-context-management.md`: chốt `get_turn_page(anchor_turn_id, count, direction) → {records, has_more}`, xóa dòng "trả toàn bộ", thêm dependency ngược tới #15 |
| 2 | S2 không có giới hạn bộ đệm | Thêm D.3b (formula + variables + edge cases + rationale), Core Rule #3, UI Requirements, knob `live_window_turns=15`, AC-48/AC-49 |
| 3 | Khả năng-với-tới 3 bút tích | Core Rule #3 + Visual/Audio mục 1 + UI Requirements: nhân bản 「Thẻ」「Lục」「Mục」 xuống cạnh khu nhập hành động; AC-31 cập nhật "5 vị trí render" |
| 4 | D.6 dải an toàn tự mâu thuẫn với invariant | D.6 formula + Tuning Knobs: chuyển 3 dải độc lập → ràng buộc liên-knob tường minh; sửa đáy `transition_screen_ms` (150→≥`card_transition_ms`); AC-27 nâng `[Config]`→`[Unit]` BLOCKING |
| 5 | Thiếu giá trị contrast/alpha cụ thể | Visual/Audio mục 9: bảng 3 bậc alpha (1.0/0.60/0.38) đạt ≥4.5:1, focus indicator chốt cố định 2px |
| 6 | D.4 range `hit_height/hit_width` sai khi glyph >44px; thiếu định nghĩa `distance_to_window_edge()` | Sửa range thành `[max(w,h), max(w hoặc h, TOUCH_TARGET_MIN)]`; bổ sung định nghĩa + biến `distance_to_window_edge`, `edge_turn_id`, thêm INVARIANT `PREFETCH_THRESHOLD < PAGE_SIZE` vào D.3 |
| 7 | Tổng AC `[Unit]` sai (21 vs 25 thực tế) | Đếm lại đúng: 25 gốc + AC-27 nâng hạng + AC-47/48/49 mới = **28 [Unit] / 49 AC tổng** |
| 8 | Guard `new_slot_created` thiếu test âm-tính | Thêm AC-47 (mục 15 mới) |
| 9 | `reset_in_progress` chưa định nghĩa ở #13 | Đổi mọi tham chiếu (D.1 GAP-4, Edge Cases, AC-41, ghi chú test setup) sang state đã tồn tại `state = "Processing Chơi Lại"` / `"Reset Failed"` của #13 — không cần sửa file #13 |
| 10 | ADR screen stack thiếu cảnh báo kỹ thuật | Open Question #5: thêm 2 cảnh báo bắt buộc (không dùng `change_scene_to_*`; safe-area insets không phải API Godot built-in cho Web export) |

Recommended đã tiện thể đóng (chi phí thấp, nằm cùng vùng sửa blocking): sửa cách diễn đạt WCAG 2.5.5 ở D.4 (compliant-by-exception, không phải deviation); viết lại AC-40 (bỏ cụm "đầy đủ chức năng" mơ hồ); thêm invariant `S < M < L` cho `font_scale_steps`; thêm 3 Open Question mới (#9 cơ chế query state chéo hệ, #10 số phận input khi AI timeout, #11 screen reader/AccessKit).

Recommended CHƯA đóng trong lần sửa này (còn nguyên trong Recommended Revisions của báo cáo review): di dời Visual/Audio + UI Requirements sang `design/ux/*`; chữ ký chuyển động riêng cho takeover S5; AC bổ sung cho "mọi chuyển màn hình tự đóng overlay" (nhiều cạnh) và "giữ vị trí cuộn khi resize/rotate"; AC-20 tách ranh giới sở hữu D.4 vs nút Song Tu (#14); AC cho Core Rule #2 (boot → S1).

Trạng thái sau sửa: **Designed — Revised, chờ re-review**. Khuyến nghị `/clear` rồi `/design-review design/gdd/core-ui-screen-navigation.md` lại để xác nhận trước khi đánh dấu Approved.

## Review — 2026-08-04 — Verdict: NEEDS REVISION (re-review)
Scope signal: M (sửa tài liệu — scope IMPLEMENTATION không đổi, vẫn XL như vòng 1: 11 dependency, 7 formula, 5 màn hình, ≥3 ADR dự kiến)
Specialists: game-designer, systems-designer, qa-lead, ux-designer, ui-programmer, godot-specialist, accessibility-specialist, creative-director (senior synthesis)
Blocking items: 7 | Recommended: ~19 (4 nhóm: ADR-bound, vệ sinh câu chữ, AC coverage, cần playtest)
Summary: Kiến trúc lõi (mô hình 3 tầng, D.1, D.2, D.3, D.5, D.6) đứng vững qua vòng adversarial thứ 2 — không specialist nào chạm vào. 7 blocking khu trú ở 4 điểm cục bộ: (1) D.3b (`LIVE_WINDOW_TURNS`) neo theo `turns_since_screen_entered(session)` — đại lượng phụ thuộc phiên thay vì dữ liệu — khiến resume 1 slot có lịch sử dài cho khung tường thuật S2 TRẮNG TRƠN (R1); (2) "evict" ở D.3b có thể bị lẫn với luật "không free node" của tầng-màn-hình, phá chứng minh O(1) mà AC cũ không bắt được, cộng rủi ro khóa nhầm 3 bút tích Thẻ/Lục/Mục vào khóa đệ quy khu nhập (R2); (3) bảng contrast (mục 9) sai số thật — "≈7:1" thực ra ~3.9:1, hụt AA cho text đang hoạt động (R3); (4) tap-tên (tương tác nhiều nhất trong game) thiếu hoàn toàn styling dương tính (R4); (5) D.4 range "đã sửa" ở vòng 1 vẫn sai — dùng chung cận dưới cho `hit_height`/`hit_width` (R5); (6) Pending Fate (quyết định sinh-tử NPC) không có phân biệt thị giác nào trong khung 4-gợi-ý, mâu thuẫn triết lý "màu khẩu phần hiếm-mới-có-nghĩa" (R6); (7) S5 takeover thiếu chữ ký chuyển động riêng cho khoảnh khắc cao-stakes nhất game (R7). 4 bất đồng specialist được creative-director phân xử tường minh (nhân bản bút tích — giữ nguyên, thêm tripwire; D.6 liên-GDD — hạ Recommended vì AC-27 đã chặn CI; `LIVE_WINDOW_TURNS` vs Combat — hạ Recommended, cần playtest không phải redesign; API recursive-disable chưa xác minh — không blocking vì D.1 tự bảo vệ đúng chỗ).
Prior verdict resolved: Vòng 1 (10 blocking) — resolved. Ghi nhận: 2 bản vá vòng 1 CHƯA triệt để (D.4 range vẫn sai — nay sửa lại đúng theo từng biến; D.6 chỉ vá 3/4 knob, bỏ sót #14 — nay hạ Recommended + thêm cảnh báo cross-file) và 2 mục Recommended tự nhận "chưa đóng" vẫn treo (AC overlay-tự-đóng exhaustive, neo cuộn resize — mục sau nay đóng cùng R2).

## Revision — 2026-08-04 — Sửa ngay trong cùng phiên (theo lựa chọn người dùng: "Sửa GDD ngay")

3 quyết định thiết kế được chốt qua `AskUserQuestion` (đều theo phương án Recommended của creative-director) trước khi sửa:
1. Tap-tên styling dương tính → **mực đậm hơn 1 bậc**, không gạch chân/khung/màu.
2. Pending Fate trọng lượng thị giác → **đậm mực hơn + không chip intent kèm**, giữ 1-tap = chấp bút, không UI riêng/không xác nhận 2 bước.
3. S5 takeover transition → **chữ ký riêng: mực nhòe/trang lặng đi tại chỗ**, không dùng page-flip chuẩn.

| # | Blocking item | Fix applied |
|---|---|---|
| 1 (R1) | D.3b resume-from-slot-cũ hỏng — neo theo phiên thay vì dữ liệu | Đổi công thức `s2_resident_turns(session)`→`s2_resident_turns(slot) = min(total_turns(slot), LIVE_WINDOW_TURNS)`; xóa biến `session`/`turns_since_screen_entered`, bảng Variables đầy đủ; thêm mục "Nạp nội dung khi vào S2" (cold-start qua `get_turn_page`, steady-state qua Turn Manager signal); sửa Edge Cases tách rõ `total_turns=0` vs slot có lịch sử; AC-48 sửa input, AC-50 mới test cold-start resume |
| 2 (R2) | Eviction có thể bị lẫn "không free node"; rủi ro khóa nhầm bút tích | D.3b: thêm mục "Cơ chế eviction" tách bạch tường minh 2 tầng vòng đời node + "Neo vị trí cuộn khi eviction"; Godot notes: thêm ràng buộc bắt buộc 3 bút tích PHẢI ngoài subtree recursive-disable + mục "Phạm vi vòng đời node — 2 tầng KHÔNG được lẫn"; AC-49 siết lại (đếm thực thể thật, không chỉ spy) |
| 3 (R3) | Bảng contrast mục 9 sai số — 1 bậc hụt AA thật | Tính lại bằng công thức WCAG relative luminance; nâng alpha "-1 bậc" 0.60→0.68 (≈3.9:1→≈4.9:1); sửa số "mờ mực" 0.38 (≈4.6:1 sai→≈2.2:1 đúng, ghi rõ dựa miễn trừ WCAG 1.4.3) |
| 4 (R4) | Tap-tên thiếu styling dương tính | Thêm Visual/Audio mục 11 mới: đậm mực hơn 1 bậc, ranh giới rõ với gạch chân #12; AC-53 mới |
| 5 (R5) | D.4 range vẫn sai sau vòng 1 (cận dưới dùng chung cho 2 biến) | Tách range `hit_height ∈ [h, max(h,44)]` / `hit_width ∈ [w, max(w,44)]`; thêm đường lui compliant-by-exception nếu RichTextLabel không đệm được |
| 6 (R6) | Pending Fate không phân biệt thị giác | Core Rule #3b mới; cập nhật Interactions + Dependencies (Death & Consequence); AC-54 mới |
| 7 (R7) | S5 takeover thiếu chữ ký chuyển động | Visual/Audio mục 2: thêm bullet "Takeover S5 KHÔNG dùng page-flip"; AC-55 mới |

Recommended đã tiện thể đóng (chi phí thấp, cùng vùng sửa blocking): 6 AC nhãn `[Manual]`→`[Integration]` (AC-28/31/36/40/44/45, bất nhất với AC cùng bản chất); AC-51 mới (Core Rule #2 boot→S1, trước đây 0 AC); AC-52 mới (D.3 invariant `PREFETCH_THRESHOLD<PAGE_SIZE`, mirror AC-27); AC-56 mới (keyboard-only full traversal, gap coverage accessibility); mock list bổ sung `card_exists`/`new_slot_created` + path chuẩn `[Integration]`/`[Config]`; sửa overclaim "Mobile luôn 1 cột" (dòng UI Requirements, mâu thuẫn AC-25) + "padding tự động" safe-area (mâu thuẫn OQ#5b) + "Ngoại lệ hình học duy nhất" (mâu thuẫn mục 4 Save Slot); OQ#1 mở rộng thêm khía cạnh UX (không chỉ device bucket); cảnh báo cross-file D.6↔`card_transition_ms` (#14) thêm vào Tuning Knobs; cảnh báo phương pháp luận profiling D3D12-Editor-vs-Web-export thêm vào Godot notes.

Recommended CHƯA đóng (còn nguyên, hoãn hợp lý — ADR-bound hoặc cần playtest, không phải câu hỏi thiết kế): OQ#11 AccessKit in/out scope (khuyến nghị chốt sớm — trước khi khóa D.1 thành implementation, không chỉ "trước ADR"); trần font L=1.25× trên Godot Web canvas (cần verify bằng prototype); debounce/throttle relayout khi resize liên tục; API recursive-disable 4.5/4.6 chưa xác minh tên chính xác (verify trước ADR); giá trị `LIVE_WINDOW_TURNS=15` đối chiếu Combat (cần playtest, tiêu chí đã ghi ở D.6 Rationale D.3b); bất đối xứng luồng S2→S4 vs S4→S2; im lặng khi Resolving xong thành công lúc đang ở S4; bảng feedback thiếu hàng Selected/Active; focus ring fallback trên nền tối cục bộ (cross-file #14); di dời Visual/Audio + UI Requirements sang `design/ux/*` (từ vòng 1, vẫn hoãn).

Tổng AC sau sửa: **56 AC (AC-38 cross-ref) — 30 [Unit] BLOCKING**.

Trạng thái sau sửa: **Designed — Revised, chờ re-review (vòng 2)**. Khuyến nghị `/clear` rồi `/design-review design/gdd/core-ui-screen-navigation.md` lại để xác nhận trước khi đánh dấu Approved.

## Review — 2026-08-04 — Verdict: NEEDS REVISION (vòng 3, hạng nhẹ)
Scope signal: XL (không đổi — 11 dependency, 7 formula, 5 màn hình, ≥3 ADR dự kiến)
Specialists: game-designer, systems-designer, qa-lead, ux-designer, ui-programmer, godot-specialist, accessibility-specialist, creative-director (senior synthesis)
Blocking items: 6 (5 do specialist tìm + 1 nâng cấp từ Recommended theo quyết định người dùng) | Recommended: ~11 (đa số hoãn hợp lý — ADR-bound hoặc playtest-bound)
Summary: Không finding nào ở vòng 3 chạm kiến trúc — D.1/D.2/D.3/D.5/D.6 và mô hình 3 tầng đứng vững lần thứ 3 liên tiếp qua 7 specialist adversarial. Finding tụ rõ vào D.3b (2/5 blocking specialist-tìm, tiếp nối vòng 1 "thêm mới" → vòng 2 "sai neo" R1) — creative-director chẩn đoán: không phải cơ chế sai, mà D.3b được đặc tả bằng văn xuôi rải rác thay vì bảng tổng như D.1/D.2 (không sinh finding nào suốt 3 vòng) — khuyến nghị kiến trúc-tài liệu: viết D.3b thành bảng vòng đời tường minh (đã áp dụng ở bản sửa này). 6 blocking: (B1) D.3b thiếu định nghĩa vòng đời buffer S2 khi lượt confirm lúc S2 đang ẩn → S2 có thể hiển thị nội dung stale khi quay lại; (B2) `s2_oldest_resident_turn_id` giả định turn_id liền mạch sau Undo — chưa xác nhận với World Memory, lỗi ngữ nghĩa không bị AC cũ bắt; (B3) `tap_back_to_slots` tại S5 thiếu carve-out khỏi D.1 (mâu thuẫn D.1↔D.2, cùng lý do đã áp dụng cho `tap_retry_reset`); (B4) dòng dẫn biên cửa sổ D.3b dùng sai styling empty-state dù là tap-target thật, mâu thuẫn luật R4 vòng 2; (B5, quyết định người dùng — "nhịp chạm tường minh") S5 takeover thiếu khoảng lặng đọc trước khi kích hoạt, có thể kéo người chơi khỏi đoạn văn cái chết giữa chừng; (Undo-vanish, quyết định người dùng — nâng từ Recommended) nút Undo biến mất không có chữ ký chuyển động dù Player Fantasy hứa tường minh "mực đã khô". Creative-director bác bỏ/hạ cấp 4 finding sau khi tự kiểm chứng (mock list `origin_screen`; double-tap feedback — đã có qua Disabled state; Pending Fate "chỉ khác về Độ" — hạ Recommended, xử bằng tripwire playtest; marginalia dead-zone giữa cửa sổ — giữ nguyên phán quyết vòng 2, không mở lại).
Prior verdict resolved: Vòng 2 (7 blocking R1-R7) — resolved. Ghi nhận: review vòng 3 phát hiện D.3b (đã sửa ở R1 vòng 2) còn 2 gap MỚI (B1, B2) không phải do R1 sai mà do R1 chưa bao phủ hết vòng đời/ngữ nghĩa turn_id — không phải regression, là gap sâu hơn được phát hiện qua adversarial pass thứ 3.

## Revision — 2026-08-04 — Sửa ngay trong cùng phiên (theo lựa chọn người dùng: "Sửa GDD ngay")

2 quyết định thiết kế được chốt qua `AskUserQuestion` trước khi sửa:
1. S5 takeover timing (B5) → **Nhịp chạm tường minh** (khuyến nghị creative-director): thêm dòng dẫn tap-to-continue cuối đoạn văn lượt chết, người chơi chạm để chuyển sang S5 — không tự động, không chờ cuộn.
2. Chữ ký biến mất của Undo → **Nâng lên blocking, thêm ngay** (khuyến nghị creative-director): fade alpha ≤150ms trước khi gỡ khỏi layout.

| # | Blocking item | Fix applied |
|---|---|---|
| 1 (B1) | D.3b thiếu vòng đời buffer S2 khi ẩn — có thể hiển thị nội dung stale | Thêm biến `s2_last_synced_turn_id`; thêm bảng "Vòng đời nội dung S2" (5 hàng: cold-start, steady-state visible, steady-state hidden, quay lại không-đổi, quay lại có-delta); chứng minh delta luôn ≤1 (submit chỉ từ S2); AC-58 mới |
| 2 (B2) | `s2_oldest_resident_turn_id` giả định turn_id liền mạch sau Undo — lỗi ngữ nghĩa | Đổi định nghĩa từ công thức số học (`last_confirmed_turn_id − s2_resident_turns + 1`) sang state do buffer tự duy trì (bản ghi cũ nhất THẬT SỰ resident) — xóa phụ thuộc vào giả định turn_id-liền-mạch chưa từng xác nhận với #5 |
| 3 (B3) | `tap_back_to_slots` tại S5 thiếu carve-out khỏi D.1 | Mở rộng footnote GAP-4 thành danh sách 3 hành động ngoài phạm vi D.1 (`tap_retry_reset`, `tap_back_to_slots`@S5, `tap_continue_to_fate` mới) — làm rõ `tap_back_to_slots` gate khác nhau tùy màn hình nguồn (S2: D.1 áp dụng đúng; S5: không) |
| 4 (B4) | Dòng dẫn biên cửa sổ D.3b dùng sai styling empty-state | Đổi styling sang ngôn ngữ bút tích marginalia (mục 1); sửa bảng contrast mục 9 (loại dòng này khỏi nhóm empty-state); thêm bullet liên kết ở Visual/Audio mục 1; cập nhật AC-49 |
| 5 (B5) | S5 takeover thiếu khoảng lặng đọc | Core Rule #6, States table (S2-D), D.2 EDGES (S2→S5), Edge Cases (chết lúc đọc S4), AC-12 đều sửa để phản ánh nhịp chạm tường minh (KHÔNG còn chuỗi tự động S4→S2→S5); AC-57 mới; Visual/Audio mục 2 thêm đặc tả dòng dẫn tap-to-continue |
| 6 (Undo-vanish) | Undo biến mất không có chữ ký chuyển động | Visual/Audio mục 12 mới (fade alpha ≤150ms); AC-59 mới |

Recommended đã tiện thể đóng (chi phí thấp, cùng vùng sửa blocking — nhóm "phải ghi nhận" theo creative-director): viết lại OQ#11 (AccessKit xác nhận không chạy trên Web export, câu hỏi cũ hỏi sai — đổi thành giải pháp non-AccessKit, nâng ưu tiên lên "trước khi khóa pattern RichTextLabel-meta-tag"); OQ#12 mới (SC 1.4.4 Resize Text 200% có thể không đạt được — trần font 125% + khả năng Web export khóa browser zoom); Godot notes bổ sung 3 sự thật đã xác minh qua WebSearch (API `mouse_behavior_recursive`+`focus_behavior_recursive` là 2 property riêng, cả hai đều phải set; `RichTextLabel.remove_paragraph()` xác nhận khả thi cho kiến trúc buffer-chung, kèm rủi ro D.4 hit-testing mới; safe-area fail-silent nếu thiếu custom HTML shell `viewport-fit=cover`); sửa trích dẫn D.4 (tách WCAG 2.5.5 AAA/2.1 vs 2.5.8 AA/2.2, không đổi kết luận 44px); AC-10 thêm nhánh `origin_screen=S1` còn thiếu; AC-21 phạm vi hóa theo route ADR (đường lui pad=0); AC-55 tách structural/visual giống AC-54.

Recommended CHƯA đóng (hoãn hợp lý — ADR-bound hoặc playtest-bound, không phải câu hỏi thiết kế): Pending Fate tripwire nhận biết lần đầu (playtest); trần bộ nhớ TỔNG khi D.3+D.3b+O-Card cùng active (~75+ lượt, nối Memory Ceiling TBD); mở rộng OQ#9 bao execution model của `get_turn_page` (sync/async/reentrant — race S2 cold-start vs S4 concurrent); nâng ưu tiên OQ#10 (số phận ô tự do khi timeout); mở lại "đổi cơ chế" như fallback ở Rationale D.3b nếu playtest Combat thất bại; route lên technical-director/producer: `docs/registry/architecture.yaml` schema thiếu mục cho hằng số dùng chung nhiều hệ (ảnh hưởng `card_transition_ms`, `TOUCH_TARGET_MIN`); di dời Visual/Audio + UI Requirements sang `design/ux/*` (từ vòng 1, vẫn hoãn).

Tổng AC sau sửa: **59 AC (AC-38 cross-ref) — 30 [Unit] BLOCKING** (3 AC mới đều `[Integration]`/`[Manual]`, không thêm `[Unit]`).

Trạng thái sau sửa: **Designed — Revised, chờ re-review (vòng 4)**. Khuyến nghị quy trình từ creative-director: lợi tức 7-agent adversarial đang giảm rõ (10→7→5 blocking specialist-tìm qua 3 vòng, không mục nào ở vòng 3 chạm kiến trúc) — vòng re-check tiếp theo nên là **mục tiêu 2 agent** (`systems-designer` + `qa-lead`) tập trung vào bảng D.3b mới + 4 AC đã sửa (AC-10/21/49/55/57/58/59), thay vì lặp lại toàn bộ 7-agent full review.

## Review — 2026-08-04 — Verdict: NEEDS REVISION (vòng 4)
Scope signal: XL (không đổi — 11+ dependency, 7 formula, 5 màn hình, ≥3 ADR dự kiến)
Specialists: game-designer, systems-designer, qa-lead, ux-designer, ui-programmer, godot-specialist, accessibility-specialist, creative-director (senior synthesis)
Blocking items: 9 (V4-1 đến V4-9) | Recommended: ~15 (ADR-bound, cần playtest, hoặc chi phí thấp đã tiện thể đóng cùng lần sửa)
Summary: Người dùng chọn chạy full 7-agent thay vì khuyến nghị 2-agent mục tiêu của creative-director vòng 3 — quyết định này **trả về giá trị thật**, khác dự đoán "lợi tức giảm dần". Vòng 4 phát hiện 2 lỗi MỚI chạm formula lõi từng "đứng vững 3 vòng": (V4-1) `write_action_allowed` (D.1) tự mâu thuẫn — bảng phân loại tuyên bố "tĩnh, không đổi runtime" trong khi carve-out `tap_back_to_slots`@S5 (B3, vòng 3) đã âm thầm cần biết `screen` nguồn để định đoạt kết quả, miền xác định thật là 3 biến không phải 2, claim "45 tổ hợp đầy đủ" sai; (V4-2) bảng "Vòng đời nội dung S2" (D.3b) thiếu guard cho case ĐỔI SLOT (S5→S2 qua "Chơi lại") — phản chứng cụ thể dẫn tới `get_turn_page(count=-399,...)`, input âm vô nghĩa, 0/59 AC phủ. Creative-director chẩn đoán: bất ổn tụ đúng vào phần vừa sửa ở vòng liền trước (D.3b bị vá 3 vòng liên tiếp) — bài học quy trình: nên đo lợi tức trên phần DELTA chưa qua adversarial pass, không phải toàn tài liệu gốc. 2 finding nguy hiểm nhất về trải nghiệm người chơi chỉ do `ux-designer` phát hiện — (V4-3) dòng tap-to-continue trước S5 takeover là hành động BẮT BUỘC DUY NHẤT để thoát màn hình sau khi nhân vật chết, không auto-timeout, nhưng dùng ngôn ngữ thị giác được thiết kế để bị bỏ qua → rủi ro kẹt màn hình vĩnh viễn; (V4-8) màn S4 sống hoàn toàn không có control lật về S2; (V4-9) không có onboarding cho ẩn dụ marginalia, đặc biệt nguy hiểm vì Settings (cỡ chữ accessibility) là nơi duy nhất dùng quy ước ẩn này — tạo vòng lặp thất bại accessibility. Thêm: (V4-4) mâu thuẫn nội bộ hướng đậm/nhạt mực giữa bút tích thường trực và tap-target hiếm [game-designer]; (V4-5) `LIVE_WINDOW_TURNS=15` mâu thuẫn số học đã-chứng-minh với `CONTENT_EXCHANGE_ESTIMATE=30` của Combat — creative-director ĐẢO phán quyết "cần playtest" của chính mình ở vòng 2/3 [game-designer + systems-designer, đồng thuận độc lập]; (V4-6) AC-56 vi phạm trực tiếp `coding-standards.md` testability [qa-lead + accessibility-specialist]; (V4-7) review log tự nhận sai trạng thái "đã đóng" ở 2 mục (overlay-auto-close full matrix; resize/rotate anchor) [qa-lead]. Bất đồng lớn nhất được phân xử: B2 (vòng 3, `s2_oldest_resident_turn_id`) — rationale của B2 dựa trên tiền đề bị chính `turn-manager.md`/`world-memory-context-management.md` phủ định (turn_id THỰC RA liền mạch/tái sử dụng sau Undo) — cơ chế state-based vẫn giữ nguyên (đúng), chỉ rationale sai bị sửa, cộng 1 AC tripwire mới tận dụng sự thật vừa xác nhận. Godot-specialist phát hiện mâu thuẫn nội bộ dự án (`docs/engine-reference/godot/` curated nói "1 property" ngược với claim GDD "2 property" — route sang task riêng ngoài GDD).
Prior verdict resolved: Vòng 3 (6 blocking B1-B5 + Undo-vanish) — resolved. Ghi nhận điểm tích cực: đếm AC lần đầu khớp chính xác qua 4 vòng (59 AC/30 [Unit]); bảng contrast mục 9 được accessibility-specialist tính lại độc lập bằng WCAG relative luminance và khớp chính xác — không cần sửa.

## Revision — 2026-08-04 — Sửa ngay trong cùng phiên (theo lựa chọn người dùng: "Sửa GDD ngay")

3 quyết định thiết kế được chốt qua `AskUserQuestion` (đều theo phương án Recommended) trước khi sửa:
1. S5 tap-to-continue affordance (V4-3) → **Nhịp thở + đổi text**: thêm nhịp alpha chậm (~2s/chu kỳ) + đổi "…" thành "… (chạm để tiếp tục)".
2. `LIVE_WINDOW_TURNS` (V4-5) → **Nâng lên 30 + ràng buộc liên-hệ**: mặc định 30, dải an toàn mở 8–50, invariant tường minh `≥ CONTENT_EXCHANGE_ESTIMATE (#7)`.
3. Onboarding marginalia (V4-9) → **Ink-reveal + hỏi cỡ chữ chủ động**: nhịp ink-reveal 1 lần cho 3 bút tích ở lần đầu S2 render + dòng diegetic hỏi cỡ chữ ở lần đầu Save Slot Screen render.

| # | Blocking item | Fix applied |
|---|---|---|
| 1 (V4-1) | D.1 miền xác định thiếu biến `screen`, claim "45 tổ hợp" sai | Chữ ký hàm mở rộng thành `write_action_allowed(action, tm_state, screen)`; carve-out `tap_back_to_slots`@S5 hình thức hóa trực tiếp trong formula (nhánh 2) thay vì footnote văn xuôi; Output Range sửa 45→46 tổ hợp; AC-04 cập nhật ma trận |
| 2 (V4-2) | D.3b bảng vòng đời thiếu guard đổi-slot → `get_turn_page(count=-399,...)` | Thêm hàng "Chuyển sang SLOT KHÁC" vào bảng vòng đời (luôn force cold-start); thêm đoạn "Guard bắt buộc — kiểm slot identity TRƯỚC delta vs cold-start" với phản chứng cụ thể; AC-60 mới |
| 3 (V4-3) | S5 tap-to-continue thiếu affordance, rủi ro kẹt màn hình | Core Rule #6 đổi text + gán Họ B (đậm hơn); Visual/Audio mục 2 thêm bullet "nhịp thở"; AC-57 cập nhật text + thêm case chạm-trượt; AC-61 mới (discoverability) |
| 4 (V4-4) | Mâu thuẫn đậm/nhạt mực bút tích (mục 1) vs dòng dẫn D.3b/S5 | Visual/Audio mục 1 tách tường minh "Họ A — bút tích thường trực (nhẹ hơn)" vs "Họ B — tap-target chủ động hiếm (đậm hơn, cùng chiều tap-tên mục 11)" |
| 5 (V4-5) | `LIVE_WINDOW_TURNS=15` mâu thuẫn số học với Combat (`CONTENT_EXCHANGE_ESTIMATE=30`) | Nâng mặc định 15→30; Tuning Knobs dải 8–30→8–50 + invariant liên-hệ mới; D.3b Rationale viết lại (đảo phán quyết "cần playtest"); AC-48/50 cập nhật số |
| 6 (V4-6) | AC-56 vi phạm testability (nhánh không PASS/FAIL ổn định) | Tách AC-56a (testable ngay, qua bút tích 「Thẻ」) / AC-56b (BLOCKED, không tính vào tổng cho tới khi OQ#11 đóng) |
| 7 (V4-7) | Review log tự nhận sai trạng thái "đã đóng" — overlay-auto-close 1/10 cạnh, resize/rotate 0 AC; AC-20 Song Tu ranh giới treo từ vòng 1 | AC-64 mới (ma trận đủ 10 cạnh D.2); AC-65 mới (resize/rotate, trigger khác AC-49); AC-20 làm rõ phạm vi mock-only cho Song Tu |
| 8 (V4-8) | S4 sống không có control lật về S2 | Visual/Audio mục 7 + UI Requirements Story Log thêm bút tích "« Chơi tiếp"; AC-62 mới |
| 9 (V4-9) | Thiếu onboarding marginalia, vòng lặp thất bại accessibility qua Settings | Visual/Audio mục 1 thêm đoạn onboarding (ink-reveal); mục 4 Save Slot Screen thêm dòng hỏi cỡ chữ chủ động lần đầu; AC-63 mới |

Recommended đã tiện thể đóng (chi phí thấp, cùng vùng sửa blocking): D.6 cảnh báo liên-GDD mở rộng thành 2 chiều rủi ro (không chỉ tune-xuống, còn tune-lên phá `card≤screen`); Godot notes thêm ràng buộc ownership signal Turn Manager confirm (phải thuộc S2 root controller, không thuộc node evictable — rủi ro fail-silent); hạ mức tin cậy claim "API đã xác minh vòng 3" (`mouse_behavior_recursive`/`focus_behavior_recursive`) sau khi phát hiện mâu thuẫn với `docs/engine-reference/godot/` curated — route sang technical-director; D.4 "đường lui pad=0" tách 2 nhánh (miễn trừ hợp lệ vs regression kỹ thuật dán nhãn sai) + rủi ro overlay hit-box trôi khỏi glyph sau reflow; OQ#10 đóng (giữ nguyên ô tự do qua timeout); OQ#1 phần UX đóng (D.5 thêm `is_touch_primary`, mọi thiết bị cảm ứng mặc định 1-cột — AC-24/25 cập nhật); OQ#11 thêm fallback tạm (mở Card qua bút tích 「Thẻ」 cho bàn phím, chờ giải pháp ARIA/TTS); OQ#12 thêm nhánh quyết định có điều kiện (nếu browser zoom khóa → nâng scale; nếu không → ghi nhận cơ chế) + dòng verify contrast focus tại nấc L; AC-53 neo số font-weight (400/600) thay vì "1 bậc" định tính; AC-59 tách 59a (structural, `[Integration]`) / 59b (visual, `[Manual]`); D.3b sửa lại rationale B2 (claim sai về turn_id đã bị `turn-manager.md`/`world-memory-context-management.md` phủ định) + AC-66 mới (tripwire invariant khôi phục tính test-được).

Recommended CHƯA đóng (hoãn hợp lý — ADR-bound hoặc playtest-bound): hình dạng cây node cụ thể cho carve-out bút tích khỏi khóa đệ quy (ADR nên vẽ sơ đồ tường minh, không chỉ mô tả văn xuôi); ngưỡng tỷ lệ chạm-hụt thực tế cho route D.4 pad=0 (cần playtest thiết bị thật); trần bộ nhớ TỔNG khi D.3+D.3b(30)+O-Card cùng active (tăng do V4-5, cần đối chiếu `technical-director`); xác minh trực tiếp trong Godot Editor 4.6 xem recursive-disable là 1 hay 2 property (không thể đóng bằng review thuần văn bản); AC-11 guard `continuation_choice_eligible` nhánh dương chưa tự chứa; quirk timing `JavaScriptBridge.eval()` (đọc sau ≥1 frame, đọc lại khi resize) — bổ sung Open Question #5; **Luật 2 của creative-director (dọn ~40 chú thích inline lịch sử revision ra khỏi thân GDD, chuyển hết vào review log) — CHƯA thực hiện trong lần sửa này, khuyến nghị làm trước vòng 5** vì đây là đòn bẩy chất lượng cao nhất còn lại nhưng rủi ro lỗi khi thực hiện đồng thời với nội dung sửa; di dời Visual/Audio + UI Requirements sang `design/ux/*` (từ vòng 1, vẫn hoãn).

Tổng AC sau sửa: **66 AC (AC-38 cross-ref) — 32 [Unit] BLOCKING** (AC-56→56a/56b, AC-59→59a/59b tách nhãn không đổi nội dung kiểm; AC-60–66 mới; +2 [Unit] so với vòng 3's 30 — AC-53 nay có nhánh `[Unit]`, AC-66 mới).

Trạng thái sau sửa: **Designed — Revised, chờ re-review (vòng 5)**. Khuyến nghị quy trình mới từ creative-director (đảo khuyến nghị "2-agent" của chính mình ở vòng 3, sau khi đối chiếu ngược cho thấy sẽ bỏ lọt V4-3/V4-8/V4-9 — toàn bộ do `ux-designer` phát hiện): vòng 5 nên là **3-agent delta-scoped** (`systems-designer` + `qa-lead` + `ux-designer`), kèm 3 luật quy trình — (1) mọi cơ chế MỚI sinh ra kích hoạt 1 adversarial pass riêng trên chính cơ chế đó bất kể vòng đang ở mức nào; (2) dọn lịch sử revision ra khỏi thân GDD trước vòng 5 (xem Recommended chưa đóng); (3) điều kiện dừng — APPROVE khi vòng tiếp theo không còn finding chạm formula/guard, lỗ hổng điều hướng (màn hình không lối ra), hoặc vi phạm `coding-standards.md`.

## Review — 2026-08-04 — Verdict: NEEDS REVISION (vòng 5, delta-scoped)
Scope signal: XL (không đổi)
Specialists: systems-designer, qa-lead, ux-designer, accessibility-specialist (thêm vào ngoài đề xuất 3-agent gốc vì V4-9 là cơ chế mới, đúng Luật 1), creative-director (senior synthesis)
Blocking items: 14 (B1–B14) | Recommended: 10 (ADR-bound hoặc playtest-bound)
Summary: Đúng theo khuyến nghị delta-scoped của vòng 4 — KHÔNG finding nào chạm kiến trúc (D.1 lõi/D.2/D.3/D.5/D.6/mô hình 3 tầng), vòng thứ 5 liên tiếp. Nhưng creative-director tự kiểm chứng và xác nhận: **~93% blocking (13/14) nằm trong chính text vừa sửa ở vòng 4** — tỷ lệ tiêm lỗi ~1.4 lỗi mới/lỗi đóng. Quan trọng hơn số lượng: **bản chất lỗi đã đổi** từ "lỗi thiết kế" (vòng 1-2: neo sai đại lượng, contrast sai 2×, range sai) sang "lỗi sổ sách" (hằng số sót ≥1 chỗ, đếm 10 vs 11, dải knob không khớp invariant, AC tham chiếu hằng số chưa có trong registry) — creative-director ước tính 9/14 blocking thuộc 4 lớp mà 1 script đối chiếu tĩnh sẽ bắt được, chỉ 5/14 (B1, B8, B11, B12, B13) thực sự cần phán đoán thiết kế con người. 14 blocking tụ vào 6 điểm sửa: (1) D.3b+Tuning Knobs — B1 (Undo thiếu backfill, **AC-66 vừa thêm ở vòng 4 tự FAIL** ngay ở luồng chơi bình thường, phản chứng tái dựng được), B2 (dải an toàn `live_window_turns` "8–50" tự mâu thuẫn invariant mới — TÁI PHÁT đúng bug D.6 đã tự sửa), B3 (AC-60 thiếu coverage đường đổi-slot không qua "Chơi lại"); (2) D.1 — B4 (3 call-site còn chữ ký 2 tham số cũ); (3) khối AC — B5 (AC-64 tự mâu thuẫn "10 cạnh" thật là 11 + ma trận overlay×cạnh bất khả thi), B6 (`live_window_turns=15` sót 2 chỗ), B7 (font-weight/`is_touch_primary` thiếu khỏi mock list); (4) S5 tap-to-continue — B8 (dòng dẫn có thể ngoài viewport, không auto-scroll — đúng lỗ hổng điều hướng), B9 (AC-61 hand-wave + phương pháp lead-sign-off sai cho discoverability — "curse of knowledge"), B10 (biên độ nhịp thở không neo số); (5) onboarding V4-9 (cơ chế MỚI, mật độ lỗi cao nhất tài liệu ~1.5 blocking/đoạn) — B11 (tự mâu thuẫn "chỉ 1 lần" vs "mỗi slot", ink-reveal replay mỗi lần chết), B12 (ngữ nghĩa trigger hỏi cỡ chữ chưa rõ), B13 (dòng mời cỡ chữ thiếu ngưỡng contrast — **hội tụ độc lập ux-designer + accessibility-specialist**, nâng thẳng blocking không cần thêm bằng chứng theo tiền lệ hội tụ đã đúng 2/2 lần trước); (6) Story Log chrome — B14 ("khử bão hòa" S4/S4-RO không neo số).
Prior verdict resolved: Vòng 4 (9 blocking V4-1..V4-9) — resolved (kiến trúc không đổi, nhưng phần lớn bản vá sinh finding mới). Bài học quy trình được creative-director ghi nhận rõ: Luật 2 (dọn chú thích lịch sử khỏi thân GDD) đã được khuyến nghị làm TRƯỚC vòng 5 nhưng bị hoãn — ít nhất 3 blocking vòng 5 (B4, B6×2 site) là hậu quả trực tiếp của việc hoãn đó, "cái giá giờ đã đo được, không còn là suy đoán".

## Revision — 2026-08-04 — Sửa ngay trong cùng phiên (theo lựa chọn người dùng: "Sửa GDD ngay")

3 quyết định thiết kế được chốt qua `AskUserQuestion` (đều theo phương án Recommended của creative-director) trước khi sửa:
1. B1 (Undo thiếu backfill) → **Backfill**: khi window đầy trước Undo, bù thêm 1 lượt cũ hơn qua `get_turn_page`, giữ nguyên sức mạnh tripwire AC-66; thêm Undo thành hàng thứ 7 trong bảng vòng đời D.3b (không để ở Edge Cases).
2. B11 (onboarding lặp mỗi slot) → **Per-device qua `app_config`**: field `has_seen_marginalia_onboarding` mới, dùng đúng cơ chế lưu trữ đã có sẵn cho phần hỏi cỡ chữ.
3. Chiến lược vòng 6 → **3 nhịp kết hợp**: vệ sinh cấu trúc trước → sửa nội dung → prototype throwaway D.3b; vòng 6 sau đó chỉ cần 2-agent (`ux-designer` + `qa-lead`).

| # | Blocking item | Fix applied |
|---|---|---|
| B1 | D.3b Edge Case Undo thiếu backfill → AC-66 tự fail | Thêm backfill bắt buộc khi window đầy (Edge Cases + hàng 7 mới "Undo khi window đã đầy" trong bảng vòng đời); AC-66 điều kiện GIVEN làm rõ mốc "ngay sau khi thao tác trả quyền điều khiển" |
| B2 | Dải an toàn `live_window_turns` mâu thuẫn invariant `≥ CONTENT_EXCHANGE_ESTIMATE` | Đổi sàn dải an toàn từ số cố định "8" sang tương đối "`CONTENT_EXCHANGE_ESTIMATE` hiện hành – 50" (mirror cách D.6 xử lý `transition_screen_ms`); thêm dòng `INVARIANT:` hình thức vào code block D.3b; AC-67 mới (`[Unit]`, mirror AC-27/AC-52) |
| B3 | AC-60 chỉ phủ đường "Chơi lại", thiếu đường đổi-slot khác | AC-68 mới — test đường S1→slot khác, cùng guard slot-identity |
| B4 | D.1 còn 3 call-site chữ ký 2 tham số cũ | Sửa 3 vị trí (Edge Cases D.1 ×2, Edge Cases toàn cục ×1) thành `write_action_allowed(action, tm_state, screen)` |
| B5 | AC-64 tự mâu thuẫn "10 cạnh" (thật 11) + ma trận overlay×cạnh bất khả thi | Viết lại AC-64: tách 2 danh sách riêng — O-Card (9 cạnh, trừ 2 cạnh từ S1) và O-Set (5 cạnh, chỉ từ S1/S2) |
| B6 | `live_window_turns=15` (giá trị cũ) còn sót 2 chỗ | Sửa UI Requirements (dòng khung tường thuật) + "Ghi chú test setup" thành 30 |
| B7 | font-weight 400/600 và `is_touch_primary` thiếu khỏi mock list | Bổ sung cả 2 vào "Ghi chú test setup" (danh sách phụ thuộc + hằng số registry) |
| B8 | Dòng tap-to-continue S5 có thể ngoài viewport; trạng thái slot khi thoát sớm chưa định nghĩa | Core Rule #6: thêm auto-scroll bắt buộc (exception DUY NHẤT của luật "không sticky/không auto-scroll"); định nghĩa slot giữ "đang chơi" nếu thoát về S1 trước khi chạm, render lại y hệt khi vào lại |
| B9 | AC-61 hand-wave + phương pháp lead-sign-off sai cho discoverability | Tách AC-61a (`[Integration]`, cơ chế auto-scroll + biên độ đo được) / AC-61b (`[Manual]`, playtest thật ≥3 người chưa từng gặp quy ước, ngưỡng PASS cụ thể — thay "lead sign-off qua video") |
| B10 | Biên độ nhịp thở không neo số | Neo alpha dao động `[0.85, 1.0]` — đáy không chạm ngưỡng AA 0.68 đã khóa |
| B11 | Onboarding ink-reveal tự mâu thuẫn "chỉ 1 lần" vs trigger "mỗi slot" | Đổi trigger sang per-device (`app_config.has_seen_marginalia_onboarding`), không còn phụ thuộc slot; AC-63a cập nhật |
| B12 | Ngữ nghĩa trigger hỏi cỡ chữ chưa rõ (lặp tới khi thấy hay tới khi chọn?) | Chốt tường minh: lặp tới khi CHỌN (chủ đích, không phải bug) — tách field `font_size_setting` khỏi `has_seen_marginalia_onboarding`; AC-63a thêm case "bỏ qua rồi mở lại" |
| B13 | Dòng mời cỡ chữ thiếu ngưỡng contrast tối thiểu (hội tụ độc lập ux+a11y) | Đặc tả bắt buộc: alpha 1.0 (không dùng Họ A), cỡ glyph ≥ nấc M cố định (không phụ thuộc `theme_scale`), tap-target ≥44px; AC-63b mới (`[Unit]`, đo được) + AC-63c (`[Manual]`, xác nhận thị giác) |
| B14 | "Khử bão hòa" S4/S4-RO không neo số | Thêm số placeholder: giảm saturation -40%, giữ nguyên lightness |

Recommended CHƯA đóng (đúng như creative-director đã khuyến nghị hoãn sang Nhịp 1/3 của vòng 6, không phải câu hỏi thiết kế): dọn ~40 chú thích lịch sử revision ra khỏi thân GDD (Luật 2, hoãn từ vòng 4 — nay có bằng chứng chi phí cụ thể, ưu tiên cao nhất cho Nhịp 1); trích hằng số dùng lặp (alpha, font-weight, `TOUCH_TARGET_MIN`...) về 1 bảng nguồn duy nhất; script kiểm nhất quán 4 luật (hằng số không literal trùng lặp; số đếm AC khớp bảng nguồn; dải knob thỏa mọi invariant liên-knob; grep cụm hand-wave bị cấm); race BẤT ĐỒNG BỘ `get_turn_page` — OQ#9 vẫn chưa mở rộng dù đã hứa từ vòng 3 (mở lại tường minh); `is_touch_primary` chưa có Open Question cho rủi ro hybrid-device false-positive; signal ownership fail-silent chưa có AC/OQ cross-ref; AC-59a "spy timing" mơ hồ tĩnh/động; OQ#12 nhánh XL chưa nối hệ quả dây chuyền D.4/D.5; prototype throwaway D.3b (Nhịp 3, `prototypes/`) — chưa thực hiện, khuyến nghị làm trước khi coi D.3b "ổn định".

Tổng AC sau sửa: **68 AC (AC-38 cross-ref) — 34 [Unit] BLOCKING** (AC-61→61a/61b, AC-63→63a/63b/63c tách nhãn; AC-67/68 mới).

## Nhịp 1 (một phần) — 2026-08-04 — Registry hằng số dùng chung

Thực hiện phần AN TOÀN, ÍT RỦI RO của Nhịp 1 (theo khuyến nghị chiến lược 3 nhịp của creative-director): thêm bảng **"Registry hằng số dùng chung — Visual/Audio + hằng khóa khác"** ngay sau Tuning Knobs — nguồn duy nhất cho alpha (1.0/0.68/0.38/[0.85,1.0]), focus 2px, font-weight (400/600), desaturation (-40%), `TOUCH_TARGET_MIN`, `MIN_ADJACENT_GAP_PX`, `undo_depth`, `ai_call_timeout_seconds`, `live_window_turns`, `CONTENT_EXCHANGE_ESTIMATE`. Đóng vĩnh viễn lớp lỗi đã gây ≥4 blocking qua 5 vòng (R3, B6×2, B7, B10, B14) — mọi sửa giá trị tương lai chỉ cần sửa 1 chỗ + grep tên biến.

**CHƯA thực hiện** (rủi ro cao, hoãn sang phiên riêng theo quyết định người dùng): dọn ~40 chú thích lịch sử revision (dạng "*(sửa YYYY-MM-DD vòng N, blocking X: ...)*") ra khỏi thân GDD, chuyển toàn bộ narrative sang review log. Lý do hoãn: nhiều đoạn đan xen thông tin kỹ thuật hiện hành cần giữ với diễn giải lịch sử nên bỏ — cần context sạch + đọc cẩn thận từng section để không cắt nhầm, không phù hợp làm ở cuối 1 phiên đã rất dài (5 vòng review + sửa liên tục). Khuyến nghị: thực hiện ở phiên riêng, section-by-section, sau khi `/clear`.

Trạng thái sau sửa: **Designed — Revised, chờ re-review (vòng 6)**. Khuyến nghị quy trình từ creative-director: **chiến lược 3 nhịp** thay vì tiếp tục review-thuần-văn-bản — (1) vệ sinh cấu trúc trước (Luật 2 + bảng hằng số nguồn + script kiểm nhất quán, đóng vĩnh viễn lớp lỗi B6/B7/B10/B14); (2) sửa nội dung (đã thực hiện ở bản vá này); (3) prototype throwaway D.3b bắt lỗi bằng code thay vì đọc. Điều kiện APPROVE cho vòng 6: không còn finding thuộc 3 hạng mục dừng cũ, VÀ finding còn lại thuộc lớp script đã phủ, VÀ prototype D.3b chạy qua đủ 7 hàng vòng đời với AC-66 xanh. Tín hiệu thành công: vòng 6 ra ≤4 blocking, ≥80% thuộc lớp cần phán đoán thiết kế (không phải lớp sổ sách).
